// URL base da API FastAPI.
// Em desenvolvimento, aponta para o servidor local (uvicorn).
// Quando formos hospedar no servidor da empresa, trocamos aqui
// (ou usamos uma variável de ambiente do Vite - podemos evoluir
// isso depois).
export const API_BASE_URL = "http://127.0.0.1:8000";
